info:
  name: List available block types
  type: http
  seq: 1
  tags:
    - blocks

http:
  method: GET
  url: "{{baseUrl}}/api/world/blocks/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Array of block types
    request:
      url: "{{baseUrl}}/api/world/blocks/list"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "id": "minecraft:stone",
              "display_name": ""
            }
          ]
